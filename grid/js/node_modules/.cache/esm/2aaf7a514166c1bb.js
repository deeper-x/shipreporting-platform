_416‍.x([["getYear",()=>getYear],["getMonth",()=>getMonth],["getDay",()=>getDay],["getHour",()=>getHour],["getMinute",()=>getMinute]]);function getYear(value) {
    return Number(value.split("-")[0]);
}
       function getMonth(value) {
    return Number(value.split("-")[1]);
}
       function getDay(value) {
    return Number(value.split("-")[2].substr(0, 2));
}
       function getHour(value) {
    if (value.indexOf("T") != -1) {
        var hhmm = value.split("T")[1];
        return Number(hhmm.split(":")[0]);
    }
    else {
        var hhmm = value.split(" ")[1];
        return Number(hhmm.split(":")[0]);
    }
}
       function getMinute(value) {
    if (value.indexOf("T") != -1) {
        var hhmm = value.split("T")[1];
        return Number(hhmm.split(":")[1]);
    }
    else {
        var hhmm = value.split(" ")[1];
        return Number(hhmm.split(":")[1]);
    }
}
